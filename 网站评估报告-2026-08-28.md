# Max 财经数据平台 · 全面评估报告

> 评估日期：2026-08-28 ｜ 评估方式：浏览器实测（桌面 1280×800 + 移动 375×812）+ 代码审查
> 评估范围：首页、市场洞察、板块中心、个股行情、牛熊深度分析、全球历史回顾、经济分布图、产业链分析、宏观仪表盘、对比中心、404 错误页、登录态入口、响应式与无障碍抽查

---

## 一、优点

### 1. 内容深度与差异化（最强项）
1. **牛熊深度分析页具有内容护城河**：基于上证综指 1990-2026 真实历史行情（8,536 个交易日）与标普500/纳指公开数据，按「事件→成因→传导→影响」四层框架拆解，并从估值、波动率、政策工具、投资者结构、市场有效性五个维度做中美对照。该深度在同类行情站中极为罕见。
2. **全球历史时间线内容量大且结构化**：258 条全球历史事件（中/西/亚/非/美/大洋洲）、104 条精选、53 条「对今日启示」，配合康波波次标注、牛熊重演、朝代对照四个视图，把金融数据放进了历史叙事框架，人文气质突出。
3. **产业地图提供 22 条产业链、161 个环节、136 条上下游关系的力导向全景图**，支持聚焦单链下钻，与产业链分析页形成「地图 → 明细」的完整动线。

### 2. 数据严谨性与可信度设计
4. **数据源标注近乎全覆盖**：市场页标注「数据来自东方财富公开接口」，宏观页标注「东方财富数据中心（GDP/CPI/PPI/PMI/M1/M2/税收收入/70城房价）」并注明「240 期」，地图页标注「数据来自各地统计局」，板块中心标注「东方财富板块行情（push2 clist）」。用户可随时知道数据从哪来。
5. **多级容错体系真实有效**：行情数据东财→新浪→腾讯三级回退；板块 K 线限频时降级为「限频提示 + 当日涨幅/主力净流入快照 + 重试按钮」，且资金历史柱图与成分股列表不受影响继续可用——降级不是白屏，而是保留可用信息。
6. **缓存与降级状态透明**：VIX、美债利差卡片带「缓存」角标，并注明「实时拉取失败时自动降级为最新已知值（标记缓存）」「可能存在延迟」，不假装实时，诚实标注，符合「理性面对温差」的产品气质。

### 3. 交互体验与专业功能
7. **个股 K 线工具达到准专业级**：日/周/月周期切换、画线标注六件套（趋势线/水平线/垂直线/斜线/通道/矩形）、Ctrl+Z 撤销、标注导入导出、MA/BOLL/KDJ/RSI/MACD 自由叠加、连涨强跌幅开关、吸附开关，覆盖了看盘用户的进阶需求。
8. **搜索体验流畅**：输入「平安」即时联想出「平安银行 000001 / 平安电工 001359 / 中国平安 601318」，支持名称与代码双通道，placeholder 有示例引导。
9. **个股「宏观-行业-标的」三级上下文联动卡是独有设计**：展示宏观环境（复苏期·评分 65）、行业景气（行业景气·价值区）、估值定位（PE 5.5-15 便宜下调市·安全边际充足），把单票放进自上而下的分析框架，超越普通行情页。
10. **板块中心聚合度高**：一次进入即可看到 100 个行业板块的统计条（涨跌家数、主力净流入合计）、可排序全列表、资金流入/流出 Top10、涨幅热点，点击行内展开成分股 Top、点「走势」进入 K 线联动面板，动线完整。
11. **对比中心支持跨类型归一化对比**：股票·指数·ETF 任意 2-8 个组合，以起点=100 归一化呈现，支持近 60/120/250 日与导出 CSV，常用标的快捷按钮降低上手门槛。
12. **自动刷新可控**：市场页/板块中心均提供自动刷新开关 + 手动刷新按钮 + 倒计时，尊重用户对流量与打扰的控制权。

### 4. 视觉与一致性
13. **红色主题贯彻全站且符合 A 股语义**：涨红跌绿严格执行国内市场惯例，导航激活态、Tab 激活态、关键数字强调色统一，品牌识别度高。
14. **明暗双主题完整**：顶栏一键切换，layout 内联主题脚本防首屏闪烁，themeColor 分别适配浅/深色系统状态栏。
15. **Hero 区「用数据理解经济，用理性面对温差」 + 宏观温度计 62° vs 体感 49.4°（温差 +13°）**：把抽象概念转成可感知的对撞数字，是全站最有记忆点的视觉锚。

### 5. 工程与响应式基础
16. **移动端无横向溢出**：375px 视口下首页/板块中心 scrollWidth 与 innerWidth 相等，布局自适应正常（统计卡 2+1 重排、汉堡菜单出现）。
17. **语义化基础扎实**：`lang=zh-CN`、每页独立中文 title（含模板后缀）、h1 唯一、88 个按钮全部具备可访问名称、viewport-fit=cover 适配刘海屏。
18. **性能体量轻**：dev 模式下市场页 FCP ≈ 1.2s、56 个资源、传输量约 149KB；生产构建全页面编译通过，部署链路（GitHub→Vercel 自动部署）已打通。

---

## 二、缺点

### 1. 核心功能可用性
1. **板块 K 线限频失败率高（最严重）**：实测点击「基础化工 → 走势」，K 线面板显示限频降级，点击「重试」后依然失败。东财对 Node fetch 的分钟级窗口轮换拦截使成功率仅约 60%，意味着用户近半数时间看不到最想看的 K 线。降级 UI 虽然优雅，但「核心诉求落空」本身是最大体验损伤。
2. **外部行情占位缺乏解释**：首页标普500、纳指卡显示「—」，全球热力中伦敦金、WTI 原油亦为「—」，均为灰杠无说明，用户无法区分「加载中」「数据源不可达」「休市」三种状态。
3. **北向资金显示「合计净流入 0」具有误导性**：交易所已停止实时披露北向净买入，页面仍以大字展示「0」，无「已停止披露」说明，可能让用户误以为资金停止流入，与全站「数据严谨」人设冲突。

### 2. 错误与边缘页面
4. **404 页面是 Next.js 英文默认页**（"This page could not be found."）：与全站中文品牌完全脱节，无返回首页按钮、无搜索入口、无热门推荐，用户到此即流失。
5. **错误基础设施整体缺失**：项目不存在 `not-found.tsx`、`error.tsx`、`loading.tsx`、`sitemap.ts`、`robots.ts`、`manifest`。意味着：运行时异常没有全局兜底 UI；路由切换无统一骨架屏；搜索引擎抓取与 PWA 能力为零。

### 3. 无障碍可访问性
6. **viewport 禁用了用户缩放**：`maximumScale: 1` 使低视力用户无法双指放大文字。其本意是防 iOS 表单聚焦自动缩放，但正确解法是保证输入框 font-size ≥16px，而不是牺牲缩放（WCAG 1.4.4 明确要求允许文本缩放至 200%）。
7. **表格无障碍细节不足**：15 个 `<th>` 无一设置 `scope` 属性，复杂行列在屏幕阅读器下关联性差；无「跳转到主内容」链接，键盘用户每次换页都要 Tab 穿过顶部行情条与导航。
8. **符号按钮语义弱**：自选按钮「☆」「★」直接以符号作为可访问名称，屏幕阅读器读作「星号」，无 aria-label 说明「加入自选/移出自选」。

### 4. 移动端体验
9. **数据表格横向滑动无提示**：板块行情表在 375px 下只露出「板块/涨跌幅/主力净流入」约 2.5 列，右侧还有净占比、成交额、走势按钮，但无滑动提示、无渐变遮罩、无列优先级处理，用户容易不知道右侧还有内容。
10. **全球热力矩阵移动端退化为单列长列表**：一个指数占满一行，8 个指数就要滚三屏，信息密度骤降，更适合横向滚动卡片或双列网格。
11. **顶部行情条被截断**：桌面端末项「道琼斯 53f…」直接切断，移动端「创业板」只露半字；无跑马灯滚动、无「滑看更多」暗示，截断处就是体验断点。

### 5. 导航与信息架构
12. **面包屑实现不完整**：板块中心、经济地图、牛熊分析等页面包屑只剩「首页」一级（访问同级也没有当前页名），与市场页「首页/市场洞察/大盘指数」三级面包屑不一致，层级感知断裂。
13. **产业链全景图全开时可读性差**：25 条链 161 个环节同时渲染，节点标签互相重叠、连线交叉密集，首屏像一团乱麻；虽有「聚焦产业链」下拉，但默认状态劝退。
14. **首页模块纵深长且缺少快捷导航**：数据终端多个模块纵向堆叠，滚到深处后没有锚点菜单/返回顶部，来回定位成本高。

### 6. 代码健康度
15. **主题防闪烁脚本触发 React 开发警告**：`layout.tsx` 在组件树内以 `dangerouslySetInnerHTML` 渲染 `<script>`，dev 模式控制台报 "Scripts inside React components are never executed"（生产 SSR 不受影响，但 dev Issues 面板常驻红标，干扰开发体验）。
16. **本地开发体验噪音**：dev 模式「2 Issues」标记长期驻留，且东财限频问题在 dev 日志中反复刷屏，缺少统一的数据源失败聚合日志。

---

## 三、改进建议（按优先级）

### P0 —— 核心可用性，建议立即处理

**1. 解决板块 K 线限频：服务端缓存 + 旧数据兜底（stale-while-revalidate）**
- 做法：聚合 API 增加内存/SQLite 缓存层——首次成功拉取的板块 K 线落盘并带时间戳；后续请求命中缓存直接返回；缓存未命中且东财被拦时，返回「最后成功数据 + 数据时点标注」，而非空数组。前端重试从「立即重试」改为「指数退避 + 60s 后自动重取」。
- 理由：K 线是板块中心的第一诉求，60% 成功率不可接受；且板块日线数据分钟级更新完全允许 1-5 分钟缓存，缓存命中率可近乎 100%。
- 预期：K 线可用率 60% → 95% 以上，同时大幅降低对东财的请求压力，反向减少被拦截概率。

**2. 补齐错误页四件套：not-found / error / loading / sitemap+robots**
- 做法：新建中文品牌化 404（含返回首页、搜索框、四大板块热门入口）；`error.tsx` 全局兜底（含「数据源临时不可用」话术与重试）；核心路由加 `loading.tsx` 骨架屏；补 `sitemap.ts` 与 `robots.ts`。
- 理由：错误页是用户迷路时的最后一道承接；sitemap/robots 是 SEO 从 0 到 1 的前提。
- 预期：异常场景体验闭环，搜索引擎可收录，长尾流量入口打开。

**3. 北向资金卡片改为诚实披露**
- 做法：检测到净买入为 0/空时，显示「实时披露已停止（2024-08 起）· 仅披露成交总额」，数值区替换为成交额或隐藏；tooltip 同步说明。
- 理由：「合计净流入 0」是事实性误导，伤害的是全站最难积累的「数据可信」资产。
- 预期：消除误导，强化「理性、可溯源」的品牌一致性。

### P1 —— 体验打磨，建议本迭代内完成

**4. 恢复用户缩放，改用字号方案防表单缩放**
- 做法：移除 `maximumScale: 1`，全局检查 input/textarea/select 的 font-size ≥ 16px（Tailwind 下对表单控件统一 `text-base`）。
- 预期：满足 WCAG 1.4.4，低视力用户可放大；iOS 聚焦缩放问题同步消除。

**5. 移动端表格与热力矩阵适配**
- 做法：板块表加右侧渐变遮罩 + 首次显示「← 左右滑动查看更多」气泡提示；全球热力在 <640px 切换为横向滚动卡片或双列网格；顶部行情条改为无缝跑马灯（CSS animation，hover 暂停）。
- 预期：移动端信息触达率显著提升，截断断点消失。

**6. 面包屑补全**
- 做法：为 /sector、/map、/analysis/bullbear、/compare 等页传入完整层级（如「首页/市场洞察/板块中心」），复用现有 Breadcrumbs 组件的数据源即可。
- 预期：全站层级感知一致，同时为结构化数据（BreadcrumbList schema）打基础。

**7. 产业链全景图默认聚焦**
- 做法：默认进入「聚焦单链」模式（如默认选中热度最高链），「全部」视图加聚类布局或按上中下游分区排布，节点标签防碰撞。
- 预期：首屏可读性质变，「地图 → 单链 → 环节」的探索动线更顺。

**8. 外部行情「—」占位补充状态语义**
- 做法：「—」旁加小字「源不可达/休市/加载中」三态文案，或统一 skeleton→error 组件。
- 预期：消除「数据是不是坏了」的疑虑，配合多源容错形成完整叙事。

### P2 —— 标准与长期建设

**9. 无障碍专项**：`th scope="col/row"` 全量补齐；加 skip-to-content 链接；「☆/★」加 aria-label（加入自选/移出自选）；对红色文字做一次对比度抽查（重点：浅红背景上的红字、暗色模式下的跌幅绿）。
**10. SEO/PWA 基建**：manifest + OG 图（Hero 温度计卡片就是天然分享图）；核心页补 `description` 与结构化数据。
**11. 主题脚本改写**：改用 `next/script` 的 `beforeInteractive`，或保留 SSR head 直出写法但迁移到 body 首个节点，消除 dev 模式 React 警告，清空 Issues 面板。
**12. 生产性能基线**：部署后跑一次 Lighthouse CI 记录基线（FCP/LCP/TBT），重点看 ECharts 按需引入与首页行情条轮询频率；建立「数据源失败聚合日志」替代 dev 控制台刷屏。

---

## 四、总评

该站最大的竞争力不在行情本身，而在**「历史纵深 + 数据诚实」的内容组合**：牛熊四层框架、康波标注、温度计温差、逐处标注的数据来源，构成了同类产品少有的人文气质与可信度。工程底子（容错、降级、语义化）也明显好于平均水平。

当前最需要修复的是三件「与品牌自伤相关」的事：**板块 K 线成功率（核心诉求落空）、北向资金「0」的误导、英文默认 404**。这三项都在 P0 列表，工作量不大但收益立竿见影。完成 P0 + P1 后，该站可以从「内容出色的工具站」进一步迈向「可长期沉淀用户的专业平台」。
